define([
  'app'
], function (app) {

  app.registerController('CtrlBody', [
    '$scope',
    '$rootScope',
    '$state',
    '$filter',
  function ($scope, $rootScope, $state, $filter) {

  }]);
});